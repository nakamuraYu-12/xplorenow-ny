<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card m-auto">
        <div class="card-body">
          <h4 class="card-title mb-3">アカウント削除確認</h4>
          <p class="card-text">本当にアカウントを削除しますか？</p>
          <p>この操作は取り消せません。</p>
          <div class="float-end">
            <%= link_to "削除する", withdrawal_path(current_user.id), method: :patch, data: { confirm: "本当に削除しますか？"}, class: "btn btn-danger" %>
            <%= link_to "キャンセル",  accounts_path, class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
